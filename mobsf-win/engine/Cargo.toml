[package]
name = "mobsf-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MobSF-Win analysis engine: pure-Rust APK manifest parser + external tool orchestration"

[dependencies]
zip = { version = "2", default-features = false, features = ["deflate"] }
thiserror = "1"
serde = { workspace = true }
